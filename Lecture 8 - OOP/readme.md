# 1. class 
-- blueprint/template to create objects


# 2. objects 
-- jb koe obj create huta tu constructor agr create na bhi hu tb bhi python automatically constructor create krta and usy execute krta


# 3. constructor 
-- always contain a parameter/ first parameter self huta ju obj create huta usy js variable mn store krty wo variable and self same location ku refer krty means dunun same huty
-- constructor contain multiple parameters
-- jitny objects hun gy utny constructor call hun gy



# Categories of constructor (two)
- 1. default constructor -- self (agr isku hm create ni krty tu python khud create kr de ga)
- 2. parameterized constructor -- contain multiple parameters

- (agr aik class mn multiple constructor hun tu wo execute huga ju hmare parameters se match hu rha)


# Attributes (two)
- 1. class attribue -- that is common to all objects   EX: Student.collegeName 
- 2. instance/obj attribute -- that is different for all objects   EX: self.name

- agr class and instance attribute same hun then (obj attr > class attr)



- class contain 2 thing --- 1. Attributes  2. Methods
- Attribues --- characteristics/properties
- Methods --- functions


# Static methods
- @staticmethod
- agr self parameter use ni krty then us method ku static bna dety


# Pillars of OOP (four)
- 1. abstraction -- show only imp details
- 2. encapsulation -- class+attributes+methods
- 3. inheritence
- 4. polymorphism


# 4 types of Inheritence
- Single — 1 parent, 1 child
- Multilevel — chain A→B→C
- Multiple — 2 parents, 1 child
- Hierarchical — 1 parent, 2+ children